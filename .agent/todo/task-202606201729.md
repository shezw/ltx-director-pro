## 场景

story-script-import

### 开发目标

#### 1. 开发内容

- 修复旧 long-auto workflow 导入时 LTXDirector widget 顺序错位的问题。
- 导入旧 long-auto timeline 时清理旧分段 memory，并迁移到新版输出 prefix。
- 提升旧 story entry 应用到新版图时的节点匹配兼容性。

#### 2. 预期结果

- `/Volumes/cmfui/user/default/workflows/long-auto-issue.json` 能转换为新版 story script。
- `timeline_data` 能正确保留 segments / prompts / audio / camera 等内容。
- 导入后不继承旧 `longAutoMemory` 完成状态。

#### 3. 验证方式

- `node --check js/story_script.js`
- 使用 Node VM 对真实 `long-auto-issue.json` 执行静态转换与应用仿真。
- `git diff --check`
