# Task

Scene: meta-info-ui

Target: merged-prefix-story-script-panel

## Goals

- Add a single `ShezwMetaInfo` node with only `global_prefix` visible.
- Move story script metadata into node properties.
- Keep old prefix/story nodes registered for compatibility.
- Update frontend controls to render a larger merged `Meta Info` panel.
- Migrate retained workflows to the new node.
- Update README.

## Verification

- Python compile check.
- JavaScript syntax check.
- Workflow JSON parse check.
- Workflow node structure check: one `ShezwMetaInfo`, no `ShezwGlobalPrefix`/`ShezwStoryScript`.
- Diff whitespace check.
