# Request

User requested workflow-level updates:

- Add a global prefix input to all workflows with manual input and a Gen button that fills a numeric `YYYYMMDDHHMM` + random ID.
- Sync every prefix consumer so generation and lookup use the same prefix.
- Add single-segment Re-gen to Long Auto, using a keyframe or previous segment tail frame as the start.
- Add story script import, store, and export for content-only `*-ss.json` data.
- Add an `ss_struct` field/node to describe import/export fields.
- Merge `long-auto` and `pro-console` into `ltx-director-pro`.
- Rename lip-sync and upscale workflows with the `ltx-director-pro-` prefix.
- Update `README.pro.md`.
