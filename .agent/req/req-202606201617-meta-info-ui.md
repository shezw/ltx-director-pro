# Request

User requested UI cleanup for prefix and story script controls:

- Merge prefix and story script UI into one `Meta Info` panel.
- Do not show `ss_struct`, `story_script`, `export_dir`, or `script_name` in the UI.
- Make buttons larger.
- Make `workflow_id` immutable or remove it from UI; it only needs to exist in workflow JSON.
