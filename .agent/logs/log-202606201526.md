# Log

Implemented workflow tooling changes:

- Added global prefix and story script nodes.
- Added frontend prefix and story script controls.
- Added Long Auto segment Re-gen.
- Consolidated workflow files to `ltx-director-pro`, `ltx-director-pro-lip-sync`, and `ltx-director-pro-upscale`; retained `camera` as compatibility workflow.
- Updated README with current workflow capabilities.

Validation passed:

- Python compile check passed for changed Python modules.
- JavaScript syntax checks passed for changed frontend files.
- All retained workflow JSON files parse successfully.
- Stale active workflow paths and `video/long-auto` prefixes are absent.
- `git diff --check` passed.
