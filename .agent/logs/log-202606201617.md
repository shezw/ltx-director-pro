# Log

Implemented Meta Info UI consolidation:

- Added `ShezwMetaInfo` node with only `global_prefix` as a visible widget.
- Moved `workflow_id`, script name, `ss_struct`, script cache, and export directory into node properties.
- Updated frontend story script controls into one larger Meta Info button panel.
- Migrated retained workflows from `ShezwGlobalPrefix` + `ShezwStoryScript` to `ShezwMetaInfo`.
- Updated README wording.

Validation passed:

- Python compile checks passed.
- JavaScript syntax checks passed.
- Retained workflow JSON files parse successfully.
- Each retained workflow has exactly one `ShezwMetaInfo` node and no `ShezwGlobalPrefix`/`ShezwStoryScript` UI nodes.
- Stale `GLOBAL PREFIX` / `STORY SCRIPT` UI wording is absent from README/workflow notes.
- `git diff --check` passed.
