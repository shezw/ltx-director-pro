## 阶段记录

### 做了什么

- 检查 `/Volumes/cmfui/user/comfyui_8000*.log`，确认 `ltx-director-pro` custom node 正常加载，未发现 Meta Info 导入的后端错误。
- 定位 `/Volumes/cmfui/user/default/workflows/long-auto-issue.json`，确认它是 legacy full workflow，`LTXDirector` 标题为 `C01`。
- 修复 `js/story_script.js` 的 legacy workflow 导入逻辑：
  - 已知节点类型使用固定 widget 顺序，避免 `inputs[].widget.name` 不完整导致 `widgets_values` 错位。
  - long-auto timeline 导入时剥离旧 `longAutoMemory`，避免导入后继承历史分段完成状态。
  - long-auto timeline 导入时使用新版 `ltx-director-pro` 输出 prefix。
  - 应用 story script 时增加唯一节点类型兜底匹配，兼容旧标题与新版标题不一致的图。

### 遇到的问题

- 旧 `LTXDirector.inputs` 只暴露 6 个 widget 名称，但 `widgets_values` 有 17 项；旧导入逻辑把 `5847` 当成 `timeline_data`，真正的 timeline JSON 被错过。
- ComfyUI 后端 log 不包含该导入失败，因为按钮逻辑在浏览器端执行。
- `/Volumes/cmfui/custom_nodes/ltx-director-pro` 是运行中的独立工作树，存在大量本地改动；本次先修复主仓并提交推送，不直接覆盖运行目录。

### 如何验证

- `node --check js/story_script.js`
- Node VM 使用真实 `/Volumes/cmfui/user/default/workflows/long-auto-issue.json` 执行转换与应用仿真，返回 `{ ok: true, nodes: 3, segments: 22, title: "C01", imported_from_schema: "workflow-0.4" }`。
- `git diff --check`
- `.agent/tests/results.db` 测试记录 `5` 已写入 `passed_time`。

### 遗留项

- 若要让当前运行中的 Windows/ComfyUI 挂载目录立即生效，需要同步或拉取 `/Volumes/cmfui/custom_nodes/ltx-director-pro`，并刷新浏览器前端资源。
